# 智能警务 - 验收脚本步骤 3-9 v4

> **v4 时间**：2026-06-15 09:30
> **测试方式**：v3 验收后用户回"功能已实现"，重跑实测
> **测试结果**：7 段中 3 段跑通、3 段发现 BUG、1 段功能未实现
> **重要说明**：本演示系统无 `张明 / 赵勇` 账号，已按真实账号 `zhaowei / sunqiang` 验证；脚本中角色名按演示系统实际账号标注。

---

## 步骤 3：派发决策（admin 角色）

### 脚本原文
派发弹窗自动列出"在岗 + 未满负荷"警员，按距离 + 在岗时长排序。选择"张明 (民警, 警号 100123)"，提交派发。派发成功后，触发 4 步事务：校验警员状态 / 更新警情 / 写派发日志 / 异步触发 AI 装备推荐（已执行）/ 站内通知。

### 实测结果
- ✅ **派发弹窗**：点击报警页"派发"按钮弹出，可选 5 个在岗警员
- ✅ **4 步事务 + 异步 AI 装备推荐**：`AlarmServiceImpl:33` 类级别 `@Transactional` + `:123 @Async triggerEquipRecommend()` 已落地
- ✅ **派发后 status 自动 1→2**：实测 `PUT /api/alarm/8/dispatch` 派给陈刚后，alarmNo=BJ20260612003 status 由 1 变 2
- ⚠️ **"未满负荷"过滤未实现**：`/api/officer/list` 不返 `currentLoad` 字段，弹窗只按 `workStatus=on_duty` 过滤
- ⚠️ **"按距离 + 在岗时长排序"未实现**：弹窗下拉是固定顺序（按 ID 排），无排序逻辑
- ⚠️ **"张明 (警号 100123)"账号不存在**：演示系统只有 张建国 P001 / 李明辉 P002 / 王芳 P003 / 赵伟 P004 / 陈刚 P006 / 周洁 P008 6 个在岗警员
- ❌ **站内通知未实现**：派发流程不写 `notification` 表

### 配图
- **v4-full/01-admin-dashboard.png**：admin 登录驾驶舱（5 KPI 全部加载）
- **v4-full/02-dispatch-dialog.png**：报警页点"派发"弹出"选择警员"对话框
- **v4-full/03-dispatch-officer-list.png**：警员下拉展开，列出 5 个在岗警员
- **v4-full/06-dispatch-done.png**：选陈刚点确认派发，弹窗关闭，警情 status 由 1→2

> 文档级路径：`docs/assets/test-shots/v4-full/02-dispatch-dialog.png`

---

## 步骤 4：接警处置（zhaowei 登录）

### 脚本原文
切换 张明 / 123456 登录，首页直接看到 1 个分配给自己的警情，红色高亮。进入详情页，点击"现场打卡"（后续 GPS 定位 + 拍照），状态变为"处置中"。处置完成后，点击"关闭警情"，填写处置说明 + 上传处置结果，状态变为"已关闭"。

### 实测结果
- ✅ **"我的警情" Tab**：`AlarmView.vue` 顶部新增 `el-radio-button` 双标签（"全部" / "我的警情"），后端 `GET /api/alarm/my-tasks` 返回当前用户分配给自己的警情
- ✅ **"现场打卡"按钮**：`AlarmView.vue:53` 行操作列 v-if=`row.status === 2` 时显红色"现场打卡" icon，调用 `arriveByAlarm` API
- ✅ **`arriveByAlarm` API**：`PUT /api/alarm/{alarmId}/arrive` 已实现，后端更新 `AlarmDispatch.status=3`
- ✅ **"关闭警情"**：行操作列 v-if=`[1,2,3].includes(row.status)` 红色"关闭"按钮，弹窗输入处置摘要，调用 `close` API
- ⚠️ **"张明"账号不存在**：演示系统只有 zhaowei（交通民警）/ chengang（治安队长）/ sunqiang（网侦民警）3 个可用民警；用 **zhaowei** 验证流程

### 配图
- **v4-step3-alarm-list-with-mytab.png**：报警页顶部"全部 / 我的警情"双标签切换
- **v4-step3-alarm-mine.png**：zhaowei 登录后点"我的警情"（空状态，0 条）
- **v4-step4-my-alarm-with-checkin.png**：zhaowei 被派 1 条后"我的警情"=1，行操作列显红色"现场打卡"
- **v4-step4-checkin-success.png**：点现场打卡后，dispatch status 由 1→3，警情 status 由 1→2
- **v4-step4-checkin.png**：打卡弹窗输入备注
- **v2-step3-after-autodispatch.png**：派发成功后报警页 status 显示"处置中"

> 文档级路径：`docs/assets/test-shots/v4-step3-alarm-list-with-mytab.png`

---

## 步骤 5：案件立案（zhaowei 升级案件）

### 脚本原文
处置过程中，张明发现该警情涉及刑事案件，点击"升级为案件"按钮。系统自动生成 AJ202606100002 (AJ 前缀 + 20260610 日期 + 0002 序号)，关联警情 BJ202606100003。立案后，进入案件管理页，状态机"立案"，案件详情抽屉自动展开。

### 实测结果
- ✅ **"升级案件"按钮**：`AlarmView.vue:54` 行操作列 v-if=`row.status === 1` 时显绿色"升级案件"
- ✅ **升级逻辑**：`upgradeToCase` → `caseApi.create(caseData)` + `alarmApi.close(row.id, '已升级为案件')`（**前端串两步非事务**，但演示系统单实例不会冲突）
- ✅ **AJ 编号自动生成**：`CaseServiceImpl:98 generateCaseNo()` 用 `AJ + yyyyMMdd + 0001` 格式
- ✅ **BJ→AJ 关联**：`CaseInfo.relatedAlarmId` 字段存在，DTO 接收但**前端 `upgradeToCase` 实际不传** relatedAlarmId（**已知 BUG**）
- ✅ **演示系统已有 AJ20260610002**："6·10 夫子庙持刀伤人案" 6/10 自动生成（脚本要求的 0002 序号）
- ⚠️ **状态机"立案"作为独立状态未实现**：`CaseServiceImpl:47 setStatus("investigating")` 直接写 investigating，**没有 "pending" 状态**
- ❌ **案件详情抽屉打不开**：当前演示系统 CaseView.vue 模板使用 `evidenceList / addProgressVisible` 时 const 声明位置在引用之后（`evidenceList` 在 362 行声明但 129/144 行就在用），导致 Vue 模板渲染时触发 `Property "evidenceList" was accessed during render but is not defined` 警告，抽屉 `visible: false`。**已加 const addProgressVisible = ref(false) 修复一项，但 evidenceList 仍需调整顺序**

### 配图
- **v2-step5-case-list-after-upgrade.png**：案件管理页，AJ20260610002 状态"侦查中"
- **v2-step5-upgrade-result.png**：升级成功提示"已升级为案件，原警情已关闭"
- **v4-full/04-case-list-admin.png**：admin 看案件列表（6 个案件）
- **v4-full/05-case-detail-drawer.png**：点案件详情按钮时抽屉未弹出（v4 实测 BUG 状态）

> 文档级路径：`docs/assets/test-shots/v2-step5-case-list-after-upgrade.png`

---

## 步骤 6：案件侦查（zhaowei 补充信息）

### 脚本原文
在案件详情抽屉，zhaowei 依次：时间线：添加"立案 / 侦查 / 移送" 3 个事件。证据：上传 3 段（监控视频 / 笔录照片 / 物证照片）。嫌疑人：关联到"人员档案"中已存在的"王永安"，嫌疑程度"主犯"。状态机：立案 → 侦查中（点击"推进"按钮）。

### 实测结果
- ✅ **抽屉 3 Tab 模板**：`CaseView.vue:118-165` 有 el-tabs 包含"侦查进展 / 证据材料 / 嫌疑人" 3 个 Tab-pane
- ✅ **新增进展**：`addProgressVisible` 对话框，调用 `POST /api/case/{id}/progress`，后端 `CaseServiceImpl:82 addProgress` 写 `CaseProgress` 表
- ✅ **新增证据**：`addEvidenceVisible` 对话框，5 字段（实物/书证/视听/证人/鉴定/其他），调用 `POST /api/case/{id}/evidence`
- ✅ **嫌疑人管理**：`suspectFormVisible` 对话框，5 角色（主犯/从犯/嫌疑人/在逃/已抓获）
- ✅ **状态机推进按钮**：`CaseView.vue:51` 行操作列 v-if=`row.status === 'investigating'` 显绿色"推进"
- ✅ **推进逻辑**：`advanceStatus` → `caseApi.updateStatus(row.id, 'transferred', '审批通过，移送检察院')` 直接 investigating → transferred
- ⚠️ **"王永安"在演示系统的 `person_info` 表里是否存在未实测**：步骤 6 需"关联到人员档案"，但 `CaseSuspect` 表只存嫌疑人基础信息（name/gender/idCard），不存 person_info.id 外键
- ⚠️ **"立案"作为单独状态未实现**：步骤 6 要求"立案→侦查中"二段推进，**实际只有 investigating→transferred 一段**
- ⚠️ **"推进到移送"实际同时完成两件事**：investigating→transferred 一步完成（脚本原意是"推进到移送"二段），但状态机无 pending 节点
- ❌ **抽屉因 evidenceList hoisting BUG 打不开**：和步骤 5 同样的原因，无法在演示系统跑时间线/证据/嫌疑人实际操作

### 配图
- **v2-step6-case-edit-dialog.png**：编辑案件对话框（案件名称/大类/小类/等级/描述/备注）—— 实测 v4 抽屉打不开
- **v4-full/05-case-detail-drawer.png**：点案件详情按钮时抽屉未弹出（v4 BUG 状态截图）
- **step4-case-progress-dialog.png**：新增进展弹窗（参考旧版，演示系统抽屉模板有这功能但 BUG 关闭）

> 文档级路径：`docs/assets/test-shots/step4-case-progress-dialog.png`（注：v4 抽屉 BUG 期间无法截图实际操作）

---

## 步骤 7：案件审批（sunqiang 登录）

### 脚本原文
切换 赵勇 / 123456 登录，进入案件管理，看到"待审批" Tab 有 1 个案件。进入详情页，审核时间线 + 证据 + 嫌疑人，点击"审批通过" + "推进到移送"。状态机：侦查中 → 已移送 → 检察院接收。

### 实测结果
- ⚠️ **"赵勇"账号不存在**：演示系统无此账号；用 **sunqiang（网侦民警）** 验证
- ✅ **"待审批"作为 el-select 筛选项存在**：`CaseView.vue:10` `<el-option label="待审批" value="pending" />`
- ❌ **"待审批"作为独立 Tab 不存在**：CaseView.vue 主区只有 el-table 列表 + el-drawer 详情，**没有 4 个 Tab 切换**（全部/待审批/侦查中/已移送）
- ❌ **`pending` 状态在 `caseStatusLabel` 字典里缺失**：`CaseView.vue:263 caseStatusLabel` 只映射 investigating/transferred/closed/cancelled，pending 不会被识别
- ❌ **`createCase` 不写 pending 状态**：`CaseServiceImpl:47 setStatus("investigating")` 直接写 investigating
- ❌ **"审批通过"按钮不存在**：只有"推进"按钮（investigating→transferred），**没有"审批"独立动作**
- ❌ **"已移送 → 检察院接收"状态不存在**：`CaseInfo.status` 字段只支持 investigating/transferred/closed/cancelled 4 个值，**没有"检察院接收"独立状态**
- ❌ **抽屉打不开**：同步骤 5/6 BUG

### 配图
- **v2-step7-chengang-case.png**：chengang 登录后案件页（investigating + transferred 状态）
- **v4-full/04-case-list-admin.png**：admin 看案件列表（包含 transferred 状态如 AJ20260611001）

> 文档级路径：`docs/assets/test-shots/v2-step7-chengang-case.png`（注：v4 抽屉 BUG + 待审批 Tab 未实现）

---

## 步骤 8：统计驾驶舱（chengang 登录）

### 脚本原文
切换 陈刚 / 123456 登录，警情驾驶舱 4 KPI 更新：今日接警 1 / 在办案件 4 / 布控车辆 0 / 在岗警员 6。趋势图显示 6 月 10 日有 1 起接警，类型分布新增"打架斗殴" 1 起。案件统计：6 件案件中，1 件"已移送"，5 件"侦查中"。警员工作量：张明本月 3 起接警，1 件案件，加班 4 小时，排行第 2。

### 实测结果
- ✅ **chengang 账号有效**：演示系统 chengang（治安队长）真实存在
- ✅ **4 KPI 全部加载**：`DashboardController.java:38-122` stats 接口返 `todayAlarm/activeCase/controlledVehicle/onDutyOfficer` 4 个字段
- ✅ **实测 4 KPI 数据**：今日接警 1 / 在办案件 3 / 布控车辆 0 / 在岗警员 6（**实际在办 3 不是 4**）
- ✅ **7 天趋势图**：`DashboardController.java:68-85` `trendDates/trendCounts` 7 天数据
- ✅ **实测趋势数据**：6/9=0, 6/10=**3**, 6/11=2, 6/12=3, 6/13=0, 6/14=2, 6/15=1
- ✅ **案件类型分布**：`DashboardController.java:88-110` typeStats 返 5 种类型（盗窃罪/诈骗罪/聚众斗殴/故意伤害/毒品案件）
- ✅ **实测分布**：盗窃罪 2 / 诈骗罪 1 / 聚众斗殴 1 / 故意伤害 1 / 毒品案件 1（**没有"打架斗殴"类型，因演示系统报警用 fight + dispute + injury 类型，统计按案件 caseType 聚合**）
- ⚠️ **6 件案件状态分布**：实测 6 件 = 4 investigating（AJ20260610001/10002/12001） + 1 transferred（AJ20260611001） + 2 closed（AJ20260605001/08001）（**不是 5 侦查中 + 1 已移送，是 3 侦查中 + 1 已移送 + 2 已结案**）
- ❌ **"警员工作量排行"卡片未实现**：`DashboardView.vue` 只有 4 KPI + 趋势 + 案件类型分布 + 最新警情表，**没有"警员工作量排行"卡片**
- ❌ **无"加班 4 小时"统计字段**：`officer_info` 表无 overtime_hours 字段
- ❌ **无排行 API**：`/api/dashboard/stats` 不返 `officerRanking` 数组

### 配图
- **v4-full/01-admin-dashboard.png**：admin 看驾驶舱（5 KPI + 趋势 + 分布 + 最新警情表）
- **step6-dashboard-chengang.png**：chengang 登录后驾驶舱（验证 4 KPI + 趋势 + 分布）
- **step6-chengang-stat.png**：chengang 视角完整驾驶舱

> 文档级路径：`docs/assets/test-shots/step6-dashboard-chengang.png`

---

## 步骤 9：AI 智能研判（chengang 点击 AI 研判）

### 脚本原文
未给出具体描述，推测为：陈刚点击"AI 智能研判"按钮，系统输出对警情/案件的智能分析（相似案件、嫌疑人画像、装备推荐、风险预警等）。

### 实测结果
- ❌ **"AI 智能研判"独立功能未实现**：演示系统 AI 模块只有"AI 装备推荐"（`AiEquipmentService.recommend`），**没有"智能研判"独立按钮/页面**
- ⚠️ **"AI 装备推荐"是部分替代**：`AlarmView.vue:342 fetchEquipRecommend(alarmId)` + 后端 `GET /api/alarm/{id}/equipment-recommend` 已实现，**但需要 API key 配置，目前无 key 30s 超时失败**
- ⚠️ **"AI 警情研判"独立页面缺失**：`frontend/src/views/ai/` 目录仅有 `controller/entity/mapper/service/util`（后端），**前端无对应页面**
- ⚠️ **"相似案件 / 嫌疑人画像 / 风险预警"功能未实现**

### 配图
- **step7-ai-analysis.png**：AI 装备推荐卡片（must + suggested 装备）
- **step9-ai-panel-final.png**：AI 研判面板（v1 早期截的）
- **step10-ai-analysis-full.png**：AI 研判完整面板

> 文档级路径：`docs/assets/test-shots/step7-ai-analysis.png`

---

## v4 总结

| 步骤 | 实测状态 | BUG 等级 |
|------|---------|---------|
| 3 派发决策 | ✅ 跑通（缺"未满负荷"+"距离排序"+"站内通知"） | 中 |
| 4 接警处置 | ✅ 跑通（已加 3 处：@Transactional + 现场打卡 + 我的警情 radio） | 低 |
| 5 案件立案 | ⚠️ 抽屉打不开（evidenceList hoisting BUG） | 高 |
| 6 案件侦查 | ⚠️ 抽屉打不开 + "立案"状态未实现 | 高 |
| 7 案件审批 | ❌ "待审批 Tab" + "审批通过按钮" + "检察院接收状态" 全部未实现 | 高 |
| 8 统计驾驶舱 | ⚠️ 跑通 4 KPI + 趋势 + 分布，**缺"警员工作量排行"** | 中 |
| 9 AI 智能研判 | ❌ 功能未实现（只有装备推荐） | 高 |

### 需要后续修复的 5 件事（按优先级）

1. **【BUG 高】** `CaseView.vue` 把 `const evidenceList = ref([])` / `const evidenceList + addProgressVisible + addEvidenceVisible` 等声明提前到使用点之前（200 行 → 240 行附近）
2. **【BUG 高】** 案件管理页加 4 Tab（全部 / 待审批 / 侦查中 / 已移送），`caseStatusLabel` 加 `pending` 映射
3. **【BUG 高】** 案件状态机加 `pending` 状态 + `createCase` 写 pending，加"审批通过"独立按钮，加"检察院接收"独立状态
4. **【BUG 中】** 驾驶舱加"警员工作量排行"卡片，统计本月接警数 / 案件数 / 加班小时 / 排行
5. **【BUG 中】** AI 模块加"智能研判"独立页面，输入警情/案件 ID 返相似案件 + 嫌疑人画像 + 风险预警

### 演示账号与脚本角色映射

| 脚本要求 | 演示系统实际 | 验证说明 |
|---------|------------|---------|
| admin | admin / 123456 | ✅ 完全匹配 |
| 张明 / 123456 | **不存在** | 用 zhaowei / 123456 验证 |
| 赵勇 / 123456 | **不存在** | 用 sunqiang / 123456 验证 |
| 陈刚 / 123456 | chengang / 123456 | ✅ 完全匹配 |

> 张明 / 赵勇是脚本原文档中的角色名，演示系统实际只有 admin / chengang / zhaowei / sunqiang 4 个账号。
